<p>Your authentication will be expired at <%= Time.at(access_token.expires_at) %></p>

<pre>
WEIBO_CONFIG = {
  :access_token => '<%= access_token.token %>',
  :expires => <%= access_token.expires_at %>
}

</pre>